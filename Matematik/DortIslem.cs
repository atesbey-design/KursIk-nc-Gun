using System;
using System.Collections.Generic;
using System.Text;

namespace Matematik
{
    class DortIslem
    {
        public void Topla(int sayı1,int sayı2)//Gerekli olan iki argüman sayı1 ve sayı 2 dir  bu yüzden gereklidir.
        {
            int toplam = sayı1 + sayı2;
            Console.WriteLine("Girilen iki sayının toplamı :"+toplam);  
        }
    }
}
